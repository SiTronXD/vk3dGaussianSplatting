#version 450

layout (local_size_x = 16, local_size_y = 16) in;

// SBO
struct GaussianSortData
{
	uvec4 num;
};
layout(binding = 0) buffer GaussiansSortListBuffer
{
	GaussianSortData sortData[];
} gaussiansSortListBuffer;

void main()
{
	uint index = gl_GlobalInvocationID.x;

	gaussiansSortListBuffer.sortData[index].num.w = gaussiansSortListBuffer.sortData[index].num.w + 1u;
}